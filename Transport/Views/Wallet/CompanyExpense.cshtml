@model Transport.Model.CompanyExpenseViewModel
@{
    ViewBag.Title = "Company Expense";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container">
    <div class="row">
        <div class="col-md-6 offset-md-3">
            <div class="card">
                <div class="card-header bg-danger text-white">
                    <h4>Company Expense Entry</h4>
                </div>
                <div class="card-body">
                    @using (Html.BeginForm("CompanyExpense", "Wallet", FormMethod.Post))
                    {
                        @Html.AntiForgeryToken()

                        <div class="form-group">
                            <label>Expense Category</label>
                            @Html.DropDownListFor(m => m.Category,
                                new SelectList(Model.Categories, "CategoryValue", "CategoryText"),
                                "Select Category",
                                new { @class = "form-control", required = "required" })
                        </div>

                        <div class="form-group">
                            <label>Amount</label>
                            @Html.TextBoxFor(m => m.Amount, new { @class = "form-control", type = "number", step = "0.01", required = "required" })
                        </div>

                        <div class="form-group">
                            <label>Expense Date</label>
                            @Html.TextBoxFor(m => m.ExpenseDate, new { @class = "form-control", type = "date", required = "required" })
                        </div>

                        <div class="form-group">
                            <label>Paid To (Person/Vendor Name)</label>
                            @Html.TextBoxFor(m => m.PaidTo, new { @class = "form-control" })
                        </div>

                        <div class="form-group">
                            <label>Remarks</label>
                            @Html.TextAreaFor(m => m.Remarks, new { @class = "form-control", rows = 3 })
                        </div>

                        <button type="submit" class="btn btn-danger btn-block">Save Expense</button>
                        <a href="@Url.Action("Dashboard")" class="btn btn-secondary btn-block mt-2">Cancel</a>
                    }
                </div>
            </div>
        </div>
    </div>
</div>